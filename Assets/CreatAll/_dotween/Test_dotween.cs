using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_dotween : Test_dotween_data
{
    public int num;
    // Start is called before the first frame update
    void Start()
    {
        Contents(num);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
